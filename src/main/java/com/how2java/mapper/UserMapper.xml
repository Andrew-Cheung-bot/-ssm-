<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.how2java.mapper.UserMapper">

    <select id="findByUsername" resultType="com.how2java.pojo.User">
        select * from user_ where username=#{username}
    </select>
    <insert id="addUser" parameterType="User">
        insert into user_(username,password,email,activate,code) values (#{username},#{password},#{email},#{activate},#{code})
    </insert>
    <select id="findByCode" resultType="User">
        select * from user_ where code=#{code}
    </select>
    <update id="updateStatus" parameterType="com.how2java.pojo.User">
        update user_ set activate='Y' where  username=#{username}
    </update>

</mapper>